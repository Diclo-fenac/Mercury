global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "mercury-assistant"
    metrics_path: "/api/v1/health/metrics"
    static_configs:
      - targets: ["app:8000"]
